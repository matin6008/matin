# dependencies
node_modules/

# secrets — Supabase keys live here, never commit
.env

# runtime data (database + uploaded photos) — regenerated on first run from seed/
data/

# OS / editor cruft
.DS_Store
*.log
npm-debug.log*
.vercel
